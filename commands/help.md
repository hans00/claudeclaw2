---
description: List ClaudeClaw v2 slash commands
---

Show the user the available `/claudeclaw2:*` slash commands and a one-line description for each. Read `${CLAUDE_PLUGIN_ROOT}/commands/` to enumerate them and pull each file's `description:` field from the frontmatter.

Format as a clean list — slash command on the left, description on the right.

Also mention:
- For full setup walkthrough: `/claudeclaw2:init`
- For migrating an existing v1 install: `/claudeclaw2:migrate`
- For details: see the README and `docs/` in the plugin source (`${CLAUDE_PLUGIN_ROOT}`).
